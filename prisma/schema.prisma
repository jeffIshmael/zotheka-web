generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

model UserKyc {
  id             String   @id @default(cuid())
  email          String   @unique
  firstName      String?
  lastName       String?
  phoneNumber    String?
  network        String?
  dateOfBirth    String?   // e.g. "1990-01-01"
  streetAddress  String?
  city           String?
  state          String?
  postalCode     String?
  country        String?
  idType         String?   // e.g. "passport", "drivers_license"
  idNumber       String?
  idImageFront   String?  // base64 string or url
  idImageBack    String?  // base64 string or url
  selfieImage    String?  // base64 string or url
  tosAccepted    Boolean  @default(false)
  bridgeCustomerId String? // for storing the bridge customer ID once created
  smartWalletAddress String? // stores the user's generated smart wallet address
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  manualServiceOrders ManualServiceOrder[]
}

model Transaction {
  id              String   @id @default(cuid())
  email           String
  type            String   // "DEPOSIT", "WITHDRAWAL", "PURCHASE"
  amount          Float
  usdAmount       Float?
  status          String   // "pending", "completed", "failed"
  productName     String?
  code            String?
  chargeId        String?
  network         String?
  phone           String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model ManualServiceOrder {
  id              String   @id @default(cuid())
  userEmail       String
  user            UserKyc  @relation(fields: [userEmail], references: [email])
  targetEmail     String
  targetPassword  String
  service         String   // e.g., "Spotify"
  package         String   // e.g., "Individual"
  amountPaidMwk   Float
  usdAmount       Float
  orderId         String?  @unique
  paymentStatus   String   @default("pending") // "pending", "paid", "failed"
  txHash          String?
  status          String   @default("pending") // "pending", "approved", "failed"
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
